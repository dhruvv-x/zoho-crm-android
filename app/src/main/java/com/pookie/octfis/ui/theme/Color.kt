package com.pookie.octfis.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary Palette ───────────────────────────────────────────────────────────
val CrmPrimary      = Color(0xFF3F51B5)   // Deep Indigo
val CrmPrimaryDark  = Color(0xFF283593)   // Darker Indigo
val CrmPrimaryLight = Color(0xFF7986CB)   // Soft Indigo
val CrmAccent       = Color(0xFF5C6BC0)   // Indigo Mid

// ── Light Theme: Surface & Background ─────────────────────────────────────────
val CrmBackground   = Color(0xFFF5F6FA)   // Cool off-white
val CrmSurface      = Color(0xFFFFFFFF)   // Pure white cards
val CrmSurfaceAlt   = Color(0xFFEEF0FB)   // Indigo-tinted surface

// ── Dark Theme: Surface & Background ──────────────────────────────────────────
val CrmBackgroundDark = Color(0xFF0F1120)  // Deep navy-black
val CrmSurfaceDark    = Color(0xFF1A1D30)  // Dark card surface
val CrmSurfaceAltDark = Color(0xFF252840)  // Slightly lighter dark surface

// ── Table ─────────────────────────────────────────────────────────────────────
val CrmTableHeader     = Color(0xFF3F51B5)  // Matches primary (light)
val CrmTableHeaderDark = Color(0xFF1A237E)  // Deeper indigo for dark header
val CrmRowAlt          = Color(0xFFF0F2FC)  // Very light indigo row (light)
val CrmRowAltDark      = Color(0xFF1E2235)  // Dark indigo row (dark)

// ── Text ──────────────────────────────────────────────────────────────────────
val CrmOnPrimary     = Color(0xFFFFFFFF)   // White on primary
val CrmOnSurface     = Color(0xFF1A1C2E)   // Near-black (light)
val CrmOnSurfaceDark = Color(0xFFE8EAF6)   // Near-white (dark)
val CrmSubtext       = Color(0xFF6B7280)   // Neutral grey (light)
val CrmSubtextDark   = Color(0xFF9DA3B4)   // Lighter grey (dark)

// ── Utility ───────────────────────────────────────────────────────────────────
val CrmDivider      = Color(0xFFE8EAF6)   // Indigo-tinted divider (light)
val CrmDividerDark  = Color(0xFF2C3050)   // Dark divider
val CrmTabSelected  = Color(0xFF3F51B5)
val CrmTabUnsel     = Color(0xFFFFFFFF)
val CrmTabUnselDark = Color(0xFF1A1D30)

// ── Status Colors (shared) ────────────────────────────────────────────────────
val CrmSuccess = Color(0xFF43A047)   // Green
val CrmWarning = Color(0xFFFB8C00)   // Amber
val CrmError   = Color(0xFFE53935)   // Red