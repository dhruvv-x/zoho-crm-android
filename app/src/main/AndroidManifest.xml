<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <!--
        READ_PHONE_STATE and READ_CALL_LOG are no longer needed:
        we detect call completion via onResume, not via telephony broadcasts.
        Remove them to reduce the permission footprint shown to users.
    -->

    <application
        android:name=".OctfisApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Octfis">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Octfis"
            android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com.pookie.octfis"
                    android:host="oauth"
                    android:path="/callback" />
            </intent-filter>

        </activity>

        <!--
            CallStateReceiver has been removed.
            PHONE_STATE broadcasts are blocked for third-party apps on Android 9+.
            Post-call detection is now handled via Lifecycle.Event.ON_RESUME
            in ContactDetailScreen.
        -->

    </application>

</manifest>